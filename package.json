{
  "name": "webshop-with-backend",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "express-validator": "^6.9.2",
    "helmet": "^4.4.0",
    "mysql": "^2.18.1"
  },
  "scripts": {
    "watch": "nodemon --watch './backend/app.ts'",
    "dev": "tsc-watch --onSuccess \"npm run watch\"",
    "serverts": "nodemon",
    "deploy": "npm run build && sed 's/.static/static/' -i ./build/index.html && rm -rf docs && mv build docs && git add . && git commit -am deploy && git push origin main"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.14",
    "@types/mysql": "^2.15.21",
    "@types/node": "^18.11.12",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "tsc-watch": "^5.0.3"
  }
}
